#include <iostream>
#include <string>
 using namespace std;
 int main(){
    string command;
    if (command == "go") {
      cout << "GO WHERE\n"; cin >> command;

    } 
      if (command == "east"){
      cout << "\n"; cin >> command;
     }
     else if (command == "west"){
       cout << "\n"; cin >> command;
     } else if (command == "south"){
       cout << "\n"; cin >> command;
     } else if (command == "north") {
       cout << "\n";
     }
 } 
